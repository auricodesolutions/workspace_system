"use client";
import Link from "next/link";
import { FormEvent,useEffect,useState } from "react";
import { CheckCircle2,LockKeyhole } from "lucide-react";
import { API_URL } from "@/lib/auth";
export default function ResetPasswordPage(){const[token,setToken]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[message,setMessage]=useState(""),[done,setDone]=useState(false),[loading,setLoading]=useState(false);useEffect(()=>setToken(new URLSearchParams(window.location.search).get("token")||""),[]);async function submit(e:FormEvent){e.preventDefault();if(password!==confirm)return setMessage("Passwords do not match.");setLoading(true);const r=await fetch(`${API_URL}/auth/reset-password`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password})});const d=await r.json();setMessage(Array.isArray(d.message)?d.message.join(", "):d.message);setDone(r.ok);setLoading(false)}return <main className="grid min-h-screen place-items-center bg-[#eef3f0] p-5"><section className="w-full max-w-md rounded-[24px] border bg-white p-8 shadow-xl">{done?<div className="text-center"><CheckCircle2 size={48} className="mx-auto text-emerald-600"/><h1 className="mt-5 text-2xl font-bold">Password updated</h1><p className="mt-2 text-sm text-[#687570]">Your new password is ready to use.</p><Link href="/login" className="mt-6 inline-block rounded-xl bg-[#176b5b] px-6 py-3 font-bold text-white">Continue to sign in</Link></div>:<><div className="grid h-12 w-12 place-items-center rounded-xl bg-[#e8f3ef] text-[#176b5b]"><LockKeyhole/></div><h1 className="mt-5 text-2xl font-bold">Create a new password</h1><p className="mt-2 text-sm text-[#687570]">Use at least eight characters and keep it different from passwords used elsewhere.</p>{!token&&<div className="mt-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">This reset link is missing its security token. Request a new link.</div>}<form onSubmit={submit} className="mt-6 space-y-4"><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password" className="w-full rounded-xl border p-3"/><input required minLength={8} type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm new password" className="w-full rounded-xl border p-3"/>{message&&<div className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{message}</div>}<button disabled={loading||!token} className="w-full rounded-xl bg-[#176b5b] py-3.5 font-bold text-white disabled:opacity-50">{loading?"Updating...":"Update password"}</button></form></>}</section></main>}
