"use client";
import Link from "next/link";
import { FormEvent,useState } from "react";
import { ArrowLeft,Mail } from "lucide-react";
import { API_URL } from "@/lib/auth";
export default function ForgotPasswordPage(){const[email,setEmail]=useState(""),[message,setMessage]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setMessage("");try{const r=await fetch(`${API_URL}/auth/forgot-password`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const d=await r.json();setMessage(d.message||"If the account exists, a reset link has been sent.");}catch{setMessage("Unable to send the request right now. Please try again.");}finally{setLoading(false)}}return <main className="grid min-h-screen place-items-center bg-[#eef3f0] p-5"><section className="w-full max-w-md rounded-[24px] border bg-white p-8 shadow-xl"><Link href="/login" className="mb-7 flex w-fit items-center gap-2 text-sm font-semibold text-[#176b5b]"><ArrowLeft size={16}/> Back to sign in</Link><div className="grid h-12 w-12 place-items-center rounded-xl bg-[#e8f3ef] text-[#176b5b]"><Mail/></div><h1 className="mt-5 text-2xl font-bold">Forgot your password?</h1><p className="mt-2 text-sm leading-6 text-[#687570]">Enter your company email. If an active account exists, we will email a secure link that expires in one hour.</p><form onSubmit={submit} className="mt-6 space-y-4"><label className="block"><span className="mb-2 block text-xs font-bold">EMAIL ADDRESS</span><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-xl border p-3 outline-none focus:border-[#176b5b]" placeholder="you@company.com"/></label>{message&&<div className="rounded-xl bg-[#eef7f3] p-4 text-sm text-[#176b5b]">{message}</div>}<button disabled={loading} className="w-full rounded-xl bg-[#176b5b] py-3.5 font-bold text-white disabled:opacity-60">{loading?"Sending...":"Send reset link"}</button></form></section></main>}
